{% extends 'base.html.twig' %}

{% block title %}Massage{% endblock %}

{% block body %}
    <h1>Massage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ massage.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ massage.type }}</td>
            </tr>
            <tr>
                <th>HtPrice</th>
                <td>{{ massage.htPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('massage_index') }}">back to list</a>

    <a href="{{ path('massage_edit', {'id': massage.id}) }}">edit</a>

    {{ include('massage/_delete_form.html.twig') }}
{% endblock %}
